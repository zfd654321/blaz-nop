<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bl.nop.tts.dao.HourJobDao">
	<select id="queryDataDeviceHours" parameterType="java.util.Map" resultType="com.bl.nop.entity.data.DataDeviceHour">
		SELECT device_id,
		stats_date,
		stats_hour,
		SUM(used_duration) used_duration,
		SUM(stand_duration) used_duration,
		SUM(person_time) used_duration,
		SUM(depth_time) used_duration 
		FROM `bl_data_info` 
		WHERE stats_date=#{statsDate}  
		AND stats_hour =#{statsHour}  
		GROUP BY device_id
  	</select>

</mapper>